# Secrets / local — ne jamais committer
sql/create_mcp_reader.sql
env.json
# Artefacts de build (vendorises par build_and_push.sh, jamais committes)
dossiers_api_vendored.py
rerank_vendored.py

# Python
__pycache__/
*.pyc
.venv/
venv/
